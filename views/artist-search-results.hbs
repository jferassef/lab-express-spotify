{{#each artistSearched}}
<div class="card col-3" style="width: 18rem;">
  {{!-- this.images is an Array of Object, inside each of those object you have
        a "url" key.
        To display the first image in the array you could use this syntax:
        {{this.images.0.url}}
   --}}
  <img  src="{{this.images}}" class="card-img-top" alt="{{this.name}}">
  <div class="card-body">
    <h1 class="card-title">{{this.name}}</h1>
  </div>
  {{!-- Once the search is working and by exploring the data, you'll be able to 
        find what is artistIdGoesHere supposed to be 😁
   --}}
  <a href="/albums/someArtistIdGoesHere">View Albums</a>
</div>  
{{/each}}
